extends layouts/main

block content
    .authenticate-form
        form(action=`/posting${routes.postingAuthorize}`, method="post")
            input(type="text", placeholder="발급받은 ID를 입력하세요.", name="key", required=true)
            input(type="submit", value="submit")